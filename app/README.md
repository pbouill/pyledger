# CanonLedger Frontend (Vue + Vite)

Minimal frontend scaffold for CanonLedger using Vue 3 + Vite.

Quickstart:

1. cd app
2. npm install
3. npm run dev

Build:

- npm run build
- npm run preview

Notes:
- This is a lightweight starter intended to be extended (routing, components, API integration).
