# Copy this file to `.env` and fill in any local values. Do NOT commit `.env` with real secrets.

# App / build defaults
PYTHON_VERSION=3.14.2
PYTHON_TAG_SUFFIX=-slim
NODE_VERSION=24
NODE_TAG_SUFFIX=-alpine

# Database connection (passwords should be supplied via Docker secrets)
DB_HOST=db
DB_USER=postgres
DB_NAME=canonledger

# For local development you can set passwords here (NOT recommended for real repos).
# For production, supply secrets via Docker secrets and they will be exposed as files in /run/secrets/
# Example (local dev only):
DB_PASSWORD=passwords

# External ports mapping for development
APP_EXT_PORT=8888
DB_EXT_PORT=5432


# Local development DB config is handled by scripts/dev_local.sh.
# Do not set DB_TYPE or DB_PATH here; the dev runner enforces .local/dev.db (sqlite).

# Other app-specific environment
PYTHONUNBUFFERED=1
