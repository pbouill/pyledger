# Testing
pytest>=9.0.2
pytest-asyncio>=1.3.0
httpx>=0.28.1

# Linting & formatting
black>=26.1.0
ruff>=0.14.13
isort>=7.0.0
mypy>=1.19.1

# Migrations & dev
alembic>=1.18.1
pre-commit>=4.5.1

# Local env helpers
python-dotenv>=1.2.1

# Type stubs for third-party libs (helpful for editors / mypy)
types-requests>=2.32.4
types-PyYAML>=6.0.12
types-urllib3>=1.26.25
types-setuptools>=80.9.0


## SQLAlchemy 2.x is fully typed; stubs are not needed and may cause conflicts.

# ASGI/Starlette related notes — Starlette and ASGI libs ship type information; no separate stubs required
# (leave this block for future revision if explicit stubs become needed)
